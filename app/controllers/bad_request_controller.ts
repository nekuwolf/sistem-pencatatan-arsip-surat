import type { HttpContext } from '@adonisjs/core/http'

export default class BadRequestController {
    // TODO: implement propperly later, use redirect_from and query validator
    // async index({ view, request }: HttpContext) {
    //     return view.render('pages/errors/bad_request', {
    //         redirect_from: request.url()
    //     })
    // }
}