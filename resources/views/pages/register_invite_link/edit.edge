@component('components/layout/dashboard', { title: 'Edit Link Undangan', pageDescription: 'Edit Invite Link' })
  @slot('main')

    @!component('components/dashboard/breadcrumb', {
      items: [
        { label: 'Link Undangan', iconName: 'link-45deg', href: route('registerInviteLinks.index') },
        { label: 'Edit', iconName: 'pencil-square' }
      ]
    })

    {{-- Header --}}
    <div class="d-flex justify-content-between align-items-center pb-3 border-bottom mt-4 mb-4">
        <div>
            <h4 class="mb-0 fs-4">Edit Link Undangan</h4>
            <div class="text-muted small mt-1">
                <i class="bi bi-pencil me-1"></i> Ubah konfigurasi untuk Kode: <strong>{{ inviteLink.key }}</strong>
            </div>
        </div>
        <div>
             <a href="{{ route('registerInviteLinks.show', { id: inviteLink.id }) }}" class="btn btn-sm btn-outline-secondary">
                <i class="bi bi-x-lg"></i> Batal
            </a>
        </div>
    </div>

    <form action="{{ route('registerInviteLinks.update', { id: inviteLink.id }) }}" method="POST">
      {{ csrfField() }}
      {{-- Method spoofing for PUT/PATCH --}}
      <input type="hidden" name="_method" value="PUT">

      <div class="row g-3">
        
        {{-- READ ONLY SECTION --}}
        <div class="col-12 mb-2">
            <div class="alert alert-info d-flex align-items-center" role="alert">
                <i class="bi bi-info-circle-fill me-2 fs-4"></i>
                <div>
                    Anda sedang mengedit target posisi untuk kode <strong>{{ inviteLink.key }}</strong>. Kode itu sendiri tidak dapat diubah.
                </div>
            </div>
        </div>

        {{-- SECTION: Target Posisi User --}}
        <div class="col-12">
            <h6 class="fw-bold text-primary mb-0"><i class="bi bi-person-badge me-2"></i>Konfigurasi Pengguna Baru</h6>
            <hr class="mt-2 mb-0 opacity-25">
        </div>

        {{-- Organization --}}
        {{-- <div class="col-md-6">
          @!component('components/input/select', {
            id: 'newUserOrganizationId',
            name: 'new_user_organization_id',
            labelText: 'Organisasi',
            labelIcon: 'building',
            placeholder: "Pilih Organisasi",
            isRequired: true,
            enableFloatingLabel: false,
            options: organizationOptions || [],
            value: old('new_user_organization_id') || inviteLink.newUserOrganizationId,
            errorMessage: flashMessages.get('errors.new_user_organization_id') || '',
          })
        </div> --}}

        {{-- Department --}}
        {{-- <div class="col-md-6">
          @!component('components/input/select', {
            id: 'newUserDepartmentId',
            name: 'new_user_department_id',
            labelText: 'Departemen',
            labelIcon: 'diagram-3',
            placeholder: "Pilih Departemen",
            isRequired: true,
            enableFloatingLabel: false,
            options: departmentOptions || [],
            value: old('new_user_department_id') || inviteLink.newUserDepartmentId,
            errorMessage: flashMessages.get('errors.new_user_department_id') || '',
          })
        </div> --}}

        {{-- Role --}}
        <div class="col-md-6">
          @!component('components/input/select', {
            id: 'newUserRoleId',
            name: 'new_user_role_id',
            labelText: 'Role Sistem',
            labelIcon: 'shield-lock',
            placeholder: "Pilih Role",
            isRequired: true,
            enableFloatingLabel: false,
            options: roleOptions || [],
            value: old('new_user_role_id') || inviteLink.newUserRoleId,
            errorMessage: flashMessages.get('errors.new_user_role_id') || '',
          })
        </div>

        {{-- Job Role --}}
        <div class="col-md-6">
          @!component('components/input/select', {
            id: 'newUserJobRoleId',
            name: 'new_user_job_role_id',
            labelText: 'Jabatan / Posisi',
            labelIcon: 'briefcase',
            placeholder: "Pilih Jabatan",
            isRequired: true,
            enableFloatingLabel: false,
            options: jobRoleOptions || [],
            value: old('new_user_job_role_id') || inviteLink.newUserJobRoleId,
            errorMessage: flashMessages.get('errors.new_user_job_role_id') || '',
          })
        </div>

        {{-- ACTION BUTTONS --}}
        <div class="col-12 mt-4 pt-3 border-top">
           @!component('components/button/button', {
            style: 'w-100',
            type: 'submit',
            text: 'Simpan Perubahan',
            enableLoadingIndicator: true,
            variant: 'primary',
            iconName: 'save'
          })
        </div>
      </div>
    </form>
  @endslot
@end